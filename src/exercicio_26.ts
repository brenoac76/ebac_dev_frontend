const numeros = (numero1: number, numero2: number): number => numero1 * numero2

const numerosTotal = numeros(3, 5);

console.log(numerosTotal);

const saudacao = (nome: string): string => `Olá ` + nome

const saudacao2 = saudacao('Breno')

console.log(saudacao2)




